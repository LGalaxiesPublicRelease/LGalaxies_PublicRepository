struct LGalaxy {
     int Type;
      int HaloIndex;
      int SnapNum;
      float LookBackTimeToSnap;
      float CentralMvir;
      float CentralRvir;
      float DistanceToCentralGal[3];
      float Pos[3];
      float Vel[3];
      int Len;
      float Mvir;
      float Rvir;
      float Vvir;
      float Vmax;
      float ColdGasSpin[3];
      float DiskSpin[3];
      float InfallVmax;
      float InfallVmaxPeak;
      int InfallSnap;
      float InfallHotGas;
      float HotRadius;
      float OriMergTime;
      float MergTime;
      float ColdGas;
      float ColdGasRings[RNUM];
      float H2fraction;
      float H2fractionRings[RNUM];
      float StellarMass;
      float DiskMass;
      float BulgeMass;
      float DiskMassRings[RNUM];
      float BulgeMassRings[RNUM];
      float HotGas;
      float ReheatedGas;
      float EjectedMass;
      float BlackHoleMass;
           float ICM;
      float MassFromInSitu;
      float MassFromMergers;
      float MassFromBursts;
      struct metals MetalsColdGas;
      struct metals MetalsColdGasRings[RNUM];
      struct metals MetalsStellarMass;
      struct metals MetalsDiskMass;
      struct metals MetalsBulgeMass;
      struct metals MetalsDiskMassRings[RNUM];
      struct metals MetalsBulgeMassRings[RNUM];
      struct metals MetalsHotGas;
           struct metals MetalsEjectedMass;
      struct metals MetalsICM;
      float PrimordialAccretionRate;
      float CoolingRadius;
           float CoolingRate;
      float CoolingRate_beforeAGN;
      float QuasarAccretionRate;
      float RadioAccretionRate;
      float Sfr;
      float SfrRings[RNUM];
      float SfrBulge;
      float XrayLum;
      float BulgeSize;
      float DiskRadius;
      float ColdGasRadius;
      float StellarHalfMassRadius;
      float StellarHalfLightRadius;
      float CosInclination;
      int DisruptOn;
      int MergeOn;
      float MagDust[40];
      float Mag[40];
      float MagBulge[40];
      float MassWeightAge;
      float rbandWeightAge;
      int sfh_ibin;
      int sfh_numbins;
                float sfh_DiskMass[SFH_NBIN];
      float sfh_BulgeMass[SFH_NBIN];
      float sfh_DiskMassRings[RNUM][SFH_NBIN];
      float sfh_BulgeMassRings[RNUM][SFH_NBIN];
      float sfh_ICM[SFH_NBIN];
      struct metals sfh_MetalsDiskMass[SFH_NBIN];
      struct metals sfh_MetalsBulgeMass[SFH_NBIN];
      struct metals sfh_MetalsICM[SFH_NBIN];
           float sfh_DiskMass_elements[SFH_NBIN][NUM_ELEMENTS];
      float sfh_BulgeMass_elements[SFH_NBIN][NUM_ELEMENTS];
      float sfh_ICM_elements[SFH_NBIN][NUM_ELEMENTS];
      float DiskMass_elements[NUM_ELEMENTS];
      float BulgeMass_elements[NUM_ELEMENTS];
      float DiskMassRings_elements[RNUM][NUM_ELEMENTS];
      float BulgeMassRings_elements[RNUM][NUM_ELEMENTS];
      float ColdGas_elements[NUM_ELEMENTS];
      float ColdGasRings_elements[RNUM][NUM_ELEMENTS];
      float HotGas_elements[NUM_ELEMENTS];
           float ICM_elements[NUM_ELEMENTS];
      float EjectedMass_elements[NUM_ELEMENTS];
};
struct MoMaFGalaxy {
     long long GalID;
 // // ID of the galaxy     short snapnum;
 // // snapnum of the galaxy, repeated here for faster lookups of times etc     short sfh_ibin;
 // //Index of highest bin currently in use     //    float sfh_time;
 // yr // Lookback time at the middle of bin.     //    float sfh_dt;
 // yr // time width of bin.     float sfh_DiskMass;
 // 1e10 Msun/h // SFH of disk     float sfh_BulgeMass;
 // 1e10 Msun/h // SFH of bulge     float sfh_DiskMassRings[RNUM];
 //10^10 Msun/h // Star formation history in the disk RINGS.     float sfh_BulgeMassRings[RNUM];
 //10^10 Msun/h // Star formation history in the bulge RINGS.     float sfh_ICM;
 // 1e10 Msun/h // SFH of ICM     struct metals sfh_MetalsDiskMass;
 // 1e10 Msun/h // Metals locked up in stars in disk.     struct metals sfh_MetalsBulgeMass;
 // 1e10 Msun/h // Metals locked up in stars in bulge.     struct metals sfh_MetalsICM;
};
